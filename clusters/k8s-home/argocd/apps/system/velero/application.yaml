apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: velero
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  syncPolicy:
    automated:
      prune: true
    syncOptions:
    - CreateNamespace=true
  destination:
    name: in-cluster
    namespace: velero
  sources:
    - repoURL: 'https://github.com/sebastiaankok/k8s-homelab.git'
      targetRevision: main
      ref: values
      path: "clusters/k8s-home/argocd/apps/system/velero/"
      directory:
        exclude: '{application.yaml,values.yaml}'
    - chart: velero
      repoURL: 'https://vmware-tanzu.github.io/helm-charts/'
      targetRevision: "10.1.1"
      helm:
        releaseName: "velero"
        valueFiles:
          - $values/clusters/k8s-home/argocd/apps/system/velero/values.yaml
