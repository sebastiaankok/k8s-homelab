apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: zigbee2mqtt
  namespace: velero
spec:
  schedule: '@every 24h'
  skipImmediately: false
  template:
    snapshotVolumes: false
    includedNamespaces:
    - home-automation
    labelSelector:
      matchLabels:
        app.kubernetes.io/name: zigbee2mqtt
    ttl: 336h0m0s
