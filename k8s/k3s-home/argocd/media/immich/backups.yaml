apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: immich
  namespace: velero
spec:
  schedule: '@every 24h'
  skipImmediately: false
  template:
    includedNamespaces:
    - immich
    labelSelector:
      matchLabels:
        app.kubernetes.io/name: immich
    ttl: 336h0m0s
