apiVersion: v1
kind: PersistentVolume
metadata:
  name: local-immich-data-pv
  namespace: immich
  labels:
    app.kubernetes.io/name: immich
spec:
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  storageClassName: local-immich-data
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  local:
    path: /data/immich
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - k3s-home

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-immich-data-pvc
  namespace: immich
  labels:
    app.kubernetes.io/name: immich
spec:
  storageClassName: local-immich-data
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
