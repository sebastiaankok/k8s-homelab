apiVersion: v1
kind: PersistentVolume
metadata:
  name: local-sonarr-pv
  labels:
    app.kubernetes.io/name: sonarr
spec:
  capacity:
    storage: 1Gi
  volumeMode: Filesystem
  storageClassName: local-sonarr
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  local:
    path: "/data/mediaserver/sonarr"
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - k3s-home
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-sonarr-pvc
  namespace: media
  labels:
    app.kubernetes.io/name: sonarr
spec:
  storageClassName: local-sonarr
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
